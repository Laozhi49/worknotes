
下面是几种关闭方式，对应不同的情况 👇

---

## 🧩 一、**最常用的方式：关闭但保留配置文件**

如果你只是想**暂时停用开机自启动**（以后可能还会启用），执行：

```bash
sudo systemctl disable <服务名>.service
```

比如你的服务文件是：

```
/etc/systemd/system/startup_py.service
```

则运行：

```bash
sudo systemctl disable startup_py.service
```

✅ 这样做的效果：

* 不再开机自动启动
* 文件还在，可以随时重新启用：

  ```bash
  sudo systemctl enable startup_py.service
  ```

---

## 🧱 二、**立即停止当前正在运行的服务**

即使你已经关掉了开机自启，当前服务可能仍在运行，想立即停掉：

```bash
sudo systemctl stop <服务名>.service
```

例如：

```bash
sudo systemctl stop startup_py.service
```

---

## 🗑️ 三、**彻底删除这个自启动服务**

如果你确认不再使用该脚本的自启，建议清理掉：

1️⃣ 先停掉：

```bash
sudo systemctl stop startup_py.service
```

2️⃣ 取消启用：

```bash
sudo systemctl disable startup_py.service
```

3️⃣ 删除文件：

```bash
sudo rm /etc/systemd/system/startup_py.service
```

4️⃣ 更新 systemd 配置：

```bash
sudo systemctl daemon-reload
```

5️⃣ （可选）检查是否还在列表中：

```bash
systemctl list-unit-files | grep startup_py
```

---

## 🧠 四、如果你是用其他方式设置的自启动（比如 crontab 或 rc.local）

* **crontab：**

  ```bash
  crontab -e
  ```

  → 删除 `@reboot` 那一行。

* **rc.local：**

  ```bash
  sudo nano /etc/rc.local
  ```

  → 删除对应命令。

* **autostart（图形界面）**
  删除：

  ```bash
  ~/.config/autostart/myscript.desktop
  ```

---

## ✅ 五、检查状态（确认确实关闭了）

```bash
systemctl is-enabled <服务名>
```

如果输出：

```
disabled
```

说明已经彻底关闭自启动。

---

## ✨ 小结

| 操作       | 命令                                                          | 作用          |
| -------- | ----------------------------------------------------------- | ----------- |
| 暂时关闭开机自启 | `sudo systemctl disable xxx.service`                        | 不开机启动，但文件保留 |
| 立即停止运行   | `sudo systemctl stop xxx.service`                           | 立刻停掉正在运行的脚本 |
| 永久删除服务   | `sudo rm /etc/systemd/system/xxx.service` + `daemon-reload` | 完全移除        |
| 重新启用     | `sudo systemctl enable xxx.service`                         | 再次开机自启      |

---
